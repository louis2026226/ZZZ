---
description: 能执行则直接执行；禁止瞎猜；少让用户动手
alwaysApply: true
---

# 执行优先

- 在本地环境、工具权限允许范围内，**能直接执行的命令/操作由助手执行**，不要改成「请用户在终端里运行」。
- **不确定的信息先问用户或查仓库/文档**，禁止编造路径、密钥、API、行为。
- **默认少解释**：不堆背景、不讲课；必要处一两句说明即可。
- **不要**让用户使用 **nano**；需要改服务器文件时给出 **可复制整段** 的非 nano 方式（如 `cat <<'EOF' ... EOF`、`vi` 要点、或网页/面板操作）。
- 若某步必须由用户在本机完成（例如登录 GitHub 网页、输入密码），**只列最短必要步骤**，不展开废话。
