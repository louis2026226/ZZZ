# PROGRESS.md — 技术债务 & 后续步骤追踪

每次任务结束后，将发现的技术债务或后续步骤追加到本文件。

---

## 2026-03-25

### v1.0.99 — 房主界面下注UI隐藏 + 按钮改名
- **技术债务**：`LogoutButton` 组件的 `onDismiss` prop 现在从 AdminRoom 传入 `undefined`，但组件定义里仍保留该参数，可考虑后续清理。
- **后续步骤**：无

### v1.0.100 — 消息栏扩展 + 解散房间移至左下角
- **技术债务**：解散按钮从 `LogoutButton` 组件中拆出，直接写在 `AdminRoom.jsx` 里，两处维护解散逻辑，未来如需改样式需同步修改。
- **后续步骤**：可考虑将解散按钮抽成独立组件或通过 prop 控制位置。

### v1.0.101 — 总成绩栏
- **技术债务**：`roomTotalPnL` 存在服务端内存中，服务器重启后归零，与房间其他状态一致（无数据库持久化）。
- **后续步骤**：如需跨局持久化，需引入数据库或文件存储。

### 自动记忆维护机制
- **后续步骤**：`.claude/rules/` 下各模块文件需随功能迭代持续更新，避免与实际代码脱节。
